server.port=8080

logging.level.ru.practicum=INFO
logging.level.root=INFO

# Spring Datasource Configurations
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://155.212.166.137:5432/later}
spring.datasource.username=postgres
spring.datasource.password=diplom
spring.datasource.driver-class-name=org.postgresql.Driver
# Hibernate JPA Configurations
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# REMOVED target/spring-boot-boot.log line to prevent runtime write failures

